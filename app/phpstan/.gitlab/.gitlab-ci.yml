###
# PHP Stan
###
.php:stan:
  variables:
    TITLE: "phpstan"
    APP_PATH: "./app/phpstan"

php:stan:image:build:
  extends:
    - .image:build
    - .php:stan

php:stan:slim:image:build:
  extends:
    - .slim:image:build
    - .php:stan
  needs:
    - php:stan:image:build
